# archive/loop/ — finished loopruns (frozen)

One folder per looprun (`autorun-<YYYY-MM-DD_HH-MM>/`), moved here at
iteration-1 rollover. Contents: spec/summary copies + `loop_log.md` — frozen
evidence for audits/resumes. Never edit, never the source of current state
(that is the NAP + `git log`).
