// i know i do not follow the naming conventions ... i have a macro for the data and it is not up to date yet.

testing the block_transfer
- is has no sandboxing yet. so writing is possible everywhere. sandbox to worken dir of opencode and temp dir (e.g. use windows env ar TMP or TEMP in the path checks)
- needs to be tested extensively and fixed if there are problems. 
- this tool could reduce the time needed for housekeeping the todo and other text moving, copiing operation extremely.

- the agents have a tendency to use the peek tool and even if thinking for an extremely long time to base its further action on it. right now i observe a 15000 token difference. i would like to wrap the peek tool into a simple tool that can be directly fired


- also to automate the loop.log
- a tool that takes as arguments role, model, status and content.
  - then it creates automatically a new folder if loop/ is empty with the current date. 
    - so less work for the planner
  - it is usable by all agents wihout explicit management of writing acces and foler persmission for each agendt
  - the prompt for the loop tool use can

prompt of a planner if found useful:


One-shot tool-invocation test ordered by the maintainer. Call the `compact_memory` tool exactly ONCE with the argument `sessionID` = "ses_f6e042f6effe62A9iDbtoxFI7M". Pass the sessionID parameter EXPLICITLY — never omit it. Do not pass keepTokens or keepMessages (the tool defaults apply). Then in your final message report the tool's result text VERBATIM (or, if the `compact_memory` tool is not available in your tool set, report exactly that — do not guess). Do not edit any file, do not commit anything, do not write loop-log or handover lines.


One-shot tool-invocation test ordered by the maintainer. Call the `compact_memory` tool exactly ONCE. Do not pass keepTokens or keepMessages (the tool defaults apply). Then in your final message report the tool's result text VERBATIM (or, if the `compact_memory` tool is not available in your tool set, report exactly that — do not guess). Do not edit any file, do not commit anything, do not write loop-log or handover lines.
