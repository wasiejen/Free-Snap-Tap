make a proposal for a set of keywords that the maintainer can use in the repo to direct attention to.
- e.g. like --maintainer / --main already does but that are not as important right now.-
- e.g. --todo for an instruction to include it into the todo, but this is not important right now
--deferred / --defer as addition to make clear it is not for now
goal is to reduce interrupting of running tasks

inbox handling should also be a lot less immediate and discupt current operation. it is to be done when nothing else is important right now
- small items can of cource be done 

- direct maintainer message/instruction in a primary planner session have precidence

-propose a set of keywords and a prompt addition to make this clear for the planner
