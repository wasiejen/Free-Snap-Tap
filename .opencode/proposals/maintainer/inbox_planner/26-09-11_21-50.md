# 1
testing the block_transfer.ts
- is has no sandboxing yet. so writing is possible everywhere. sandbox to working dir of opencode and temp dir (e.g. use windows env ar TMP or TEMP in the path checks)
- needs to be tested extensively and fixed if there are problems. 
- this tool could reduce the time needed for housekeeping the todo and other text moving, copiing operation extremely.
- use the "agent_TempOnly" for testing if needed - only access to temp files if tool does not work
- write an instruction that is helpful for an agent to use it, so the usage is clear. if the implicit tool discription is enough for this, then use this or rewrite them to make the usage more clear.
(a generic example for this tool us found online:
#### 2. File & List Housekeeping (`block_transfer`)
* **Mandatory Rule:** NEVER use `write_file` or `str_replace` to move, copy, delete, or organize multi-line blocks, TODO lists, or log sections. ALWAYS use `block_transfer`.
* **Modes:**
  * Use `MOVE` to instantly transfer a block from a source file to a destination file using unique line anchors.
  * Use `CUT` / `COPY` $\rightarrow$ `PASTE` when manipulating multiple buffers across files.
  * Use `DELETE` to purge completed sections without outputting text.
* **Anchor Selection:** Provide short, unique line prefixes for `startMarker` and `endMarker` to slice text cleanly without generating full line contents in your tool call.)



(file a proposal with intended implementation)
  - implicitly approved to work on it (i am away for a while)

# 2
- the agents have a tendency to use the peek tool and even if thinking for an extremely long time to base its further action on it. right now i observe a 15000 token difference. i would like to wrap the peek tool into a simple tool that can be directly fired might be helpful in general to clean up the usage
(file a proposal with intended implementation)
  - implicitly approved to work on it (i am away for a while)

# 3
- automate the loop.log as tool
- a tool that takes as arguments role, model, status and content.
  - then it creates automatically a new folder if loop/ is empty with the current date. 
    - so less work for the planner
  - it is usable by all agents wihout explicit management of writing acces and foler permission for each agent
  - the prompt for the loop tool use can be simplified
(file a proposal with intended implementation)
  - implicitly approved to work on it (i am away for a while)



deferred for now:
a unified location for helpful information on custom tools and their usage might be useful. could be appended like the agent_readme on need
