current version of compact_memory is live and is now available as custom tool.
needed to rewrite it a bit to use the tool() import and then it worked. used the database custom tool as a minimal reference

on tool call by a agent it resulted in in agents answer:
"It failed: Compaction request failed: undefined is not an object (evaluating 'context.client.session') — looks like a bug in the tool's environment wiring (no session client available to the tool), not something the parameters caused. Nothing to retry on my side."
