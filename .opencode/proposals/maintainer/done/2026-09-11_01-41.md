- i might have miscomunicated before when proposing to adapt handover plugin as a tool.
  - i meant the peek functionality instead - but tabled non the less - but update this 

- more important pertaining to .opencode\proposals\maintainer\done\260911-0031.md or in new schema .opencode\proposals\maintainer\done\2026-09-11_00-31.md contains 2 optional ways to communicate the nudges. i choose the first one.
  - but i think for the agent it is more important to get constant information on every tool.execute.after appended to the message like in the mentioned note 2026-09-11_00-31.md second option. and not controlled via threshold but on each tool call return in a very minimal format like "(80%/15K)" - so the agent has always the most up to date data without needing to remember to use peak.
    - this is now more important than before as compaction is entirely deactivated, so there is not chance to exceed the context window - current information on ctx is important

- the nudges based on threshold should still fire as message but no longer in a way that causes a racing condition and context invalidation. i like to keep these emergency access points to reset/instruct an agent directly

- base the logging of context on the directly appended tool returns instead on the nudge messages. this should allow planner agents to gauge the last state of a worker. maybe for restarting if interrupted.

- sorry for adding to the idea in a gradual way. thanks.
- include this in the appropiate files like .opencode\proposals\approved\260910_plugin-compaction-detection.md
