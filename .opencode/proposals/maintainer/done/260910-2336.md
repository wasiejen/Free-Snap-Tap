#1 
we could register the handover plugin as a tool, so no shell calls would be needed and the tool could give back all values from is log of the current session - to be able to recognize self compaction and an agent to decide to end its turn even if the gauge says otherswise.


- "https://opencode.ai/docs/custom-tools/"

just an idea for now. lets discuss it if this might be to much effort for not much gain.

#2
- due to compaction and thus reseting or regressing values of the context, i think the thresholds are no longer firing because they were already triggered ones. so when a compaction is detected or the current value of a session is smaller than an old one - the threshold should be reactivated. and if compaction cannot be recognised directly, it can be recognised in this way and the information appended to the ctx information of nudges and peeks.
