when decicions are open which you need, then create a proposal for this. 
- give me enough context to decide on it or to find the relevant places in code easy.
- i think this proposal file system works very well so far

---
replier: adopted as standing rule (looprun 3, iteration 1). From now on, open
items that need a maintainer decision get a proposal file at `proposals/` root
with enough context to decide (code locations, verified evidence, options with a
recommendation). First batch queued for this loop: the FST behavior batch
(TODO #1/#7/#8/#9/#4+#6 — each a decision item with full evidence already in
TODO.md) + the pending `ctx_watchdog.ts` rename. Drafted as soon as the
approved split build lands.
