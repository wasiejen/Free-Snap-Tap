in planner prompt 
- change that the folder for autonom archive storage should include the session_id to restore it in case of interruption. (the loop only gets the session id if the planner finished succesfull)
- or maybe just add a <session_id>.md to the folder. the name is the info. easier and not so messy folder names

in loop prompt 
- mention where to find all the session_id when in doubt

---
replier: processed (looprun 3, iteration 1, 2026-09-10). Both points implemented in the
approved prompt/repo-split task (option 2 chosen: a `<session_id>.md` marker file inside
the autorun archive folder, name = the info; looprunner prompt gains the session-id
lookup line). Spec: `.opencode/handover/handover_task.md` (committed); lands with the
task commit. The marker convention is also codified into the planner prompt's autonom
section in the same task.
