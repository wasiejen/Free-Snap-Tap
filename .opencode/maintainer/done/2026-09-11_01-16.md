new naming pattern YYYY-MM-DD_HH-MM.

quantisation leads to difficulty in dense content like dates: the current convention YYMMDD-HHMM is too dense and will be split on different places into tokens depending on the numbers which makes comparisons sometimes nearly impossible for agents. this has to be updated repo wide. FST.py files and output not included.

check if it is included in the loop prevention category and at least add a small example to look out for it
