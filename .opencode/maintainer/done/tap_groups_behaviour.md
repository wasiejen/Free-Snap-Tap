--maintainer at worker2: explaination to changed tap groups behavior comment.the tab groups did not behave different - or at least not known - did not test it. the comment to the planner was my observation that in the workers last session the changed code regressed the tests for the tap groups.

clarification: rebind repeat should not work for tap group keys i think. combination of rebinds and tap groups is very unlikely. the real candidates for the function of tap groups to have a value are normally only the combination of a and d, and also w and s:
a,d
w,s

when in tap group i think they should not repeat

--comment worker2 never saw this comment

worker2 context limit. last thinking blocks around 20k token. trying to identify edgecases
