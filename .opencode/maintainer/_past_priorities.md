# _past_priorities — handled items from `priority.md` (append-only log)

Agents append ONE line per handled item, format:
`- <item as it was in the list> — <short reply>` (done / done, commit X /
see proposals/implemented/<file> / see TODO.md #N). No re-reading, no
re-editing of old entries. The maintainer owns this file — he erases entries
when he wants. The convention itself lives in `priority.md` (section
"How this works").
- # 1 compact_memory plugin that also exposes a tool (quant-class budget + CPU exclusion + optional post-compaction instruction) - done: build e07ae33, bugfix 2ace5e1, live self-compact acceptance bdc4504 (probe 98/98); residual acceptance items 3-4 (cross-session + budget-denied fire) maintainer-side; trail: proposals/approved/2026-09-12_compact_memory_plugin.md + TODO #52
- # 3 1 nap size reduction — done: Part 2 complete (NAP 1460 → 68 lines; 32 sections compressed no-loss to pointer files; Standing to measured baselines; full backup at `archive/nap_backup_2026-09-15_pre-cleanup.md`) — commit b36d4c7
- # 3 2 session dump scripts — done: `.opencode/agent/scripts/dump_session.cjs` (read-only DB; single-session FULL mode for pre-compaction dumps + `--all` corpus mode) + corpus backfilled (137 sessions → `.opencode/archive/sessions/`) — commits ab1451d + e877364; unblocks the TODO #55 dump hook (still his call)
- # 4 (planner prompt: ready-made grep command for maintainer attention markers) — done: the READY-MADE marker-sweep command lives in the planner prompt §maintainer-calls (landed plan1/looprun-1 iter-1); grep-verified in the live prompt
- # 6 (limit output lines for first greps; context is the precious resource; name the AREA in big files by lines) — done: "Context discipline on delegation" in the planner prompt (spec names the area + bounded range, first greps carry `| head -30`, read only relevant sections)
- # 9 (ctx_gauge / inline readout runs ~2 tool calls late, ~5k low) — done: "Gauge-lag rule (maintainer #9)" in the planner prompt + NAP Standing (plan with ~5k margin against the injected ctx: line)
- # 10 (script collection from temp/opencode; overview + categories + readmes) — done: `.opencode/agent/scripts/` (INVENTORY.md + README.md + db/binary/log subfolders each with README) LANDED + verified plan3 (commit 2933dd0..d352e4a); worker prompt pointers present
- 2026-09-23_04-20 marker-sweep noise — done: the fixed sweep command (include-filter before the patterns) is committed in the planner prompt (8f21b1e); re-verified clean by planner-9 2026-09-23 (no plugin.log noise, only legitimate .md hits)
- --info: optimal time for a worker's big work round = compact after planning/drafting, before the tool calls (keepMessages preserves the pre-reasoning) — done: codified in AGENTS.md `# Compaction Guidelines` (the DISTILLED-output trigger, "can be MID-UNIT", keepMessages = what you'd have to re-derive) + the role prompts' triage thresholds
- --info (the dumps seemed to not include thinking/writing; dumps should be COMPLETE; raw json as-is) — done: TODO #78 LANDED (6b33907: `--json` raw mode + lossless full markdown + `--lite` preset + the 120 s budget/retry/DUMP-OK ms= diagnostics)
- # move the complete items from priority.md into _past_priorities.md (ignore uncommitted state) — done: this curation (2026-09-25, ses_f2a436b57): 15 items moved; the partial/open items stay in the active list (status in the NAP)
- # auto-resume unit 4 should activate on a planner in autorun mode (is already a TODO, but i do not know which) — done: that is TODO #85 part 1 (unit-4 scope = actual planner sessions ∪ the `<|Autorun|>` own-line toggle; LANDED 97fccfc) + #90 (the spawned successor inherits the Autorun state + the trigger deactivates; c4b244d) — both LANDED + live-accepted 2026-09-23
- # 2026-09-23_14-25 (the spawned session should be tracked, inherit settings/state, deactivate the old session) — done: TODO #90 LANDED (c4b244d) + FULL live acceptance 2026-09-23 (route= restart spawn + ident + deactivate=)
- # 2026-09-23_14-14 (early loop_log START on worker delegation, so an interrupted worker is findable by task_id) — done: codified — the worker prompt §Instruction index: loop_log lines at SESSION START and task completion (agent_readme_loop §Loop log); the START precedes any planning
- # 2026-09-23_14-20 (compaction guideline codified in the system prompt for planner and worker) — done: AGENTS.md `# Compaction Guidelines` section (his paste, 3c390e7) + the prompt wave (the role prompts reworked compaction-oriented, wave task h)
- # 2026-09-23_14-19 (output discipline: untested commands → temp file + size check first; bounded greps) — done: the "Output discipline" bullets codified in the planner + worker prompts (plan10)
- # 2026-09-23_04-34 (autorun-spawned session names include the loop folder + the number) — done: TODO #89 LANDED + LIVE-accepted (the first named spawn 2026-09-23 13:00:08Z: `ident=autorun-… planner-10`)
- # 2026-09-23_00-12 (bounded reading; reduce the auto_resume.smoke wall-time; NO smoke in direct sessions) — done: #88 LANDED (smoke wall 145.5 s → 12.5 s, 532ddbc); context/output discipline codified in both prompts; the no-smoke-in-direct rule added to the planner prompt in this curation (2026-09-25)
- # 2026-09-22_21-13 (tests ridiculously long; planner check via a dump of the worker session's run results) — done: #88 (smoke wall-time cut) + the verify protocol as-is: the worker runs the full gate and reports measured evidence; the planner verifies from files + a targeted spot re-run (now codified for direct sessions)
- # 2026-09-23_00-10 (Stale — the loop folder storage) — done (marked Stale/DONE in-file): the loop folder is used exactly as with the looprunner (agent_readme_loop §Loop folder)
- # 2026-09-22_20-42 (NAP/TODO handling instructions directly in the system prompt; the general large-file guideline) — done: wave task h (planner Context-budget reworked) + the NAP-size discipline section + the context/output-discipline bullets in both prompts
- # can we make the auto-resume unit 4 resume messages more distinctive? — done: spec 2+11 (526e7e1) — the queued compact message is relayed FIRST + the post-compaction addendum ONLY on the compaction path; a plain stop gets the plain continueText (no addendum)
- # how to deliver the message of self-compaction and cross-compaction to the compacted session? — done: spec 2+11 (526e7e1) — the message is STORED at queue time and relayed first at resume (the unit-4 CONTINUE path)
