i do not know what happened to this tool.
the idea is to identify a section via 2 unique strings and then be able to delete, copy or replace them with (a) another section identified by 2 unique strings. could be in the same file or cross file. with the optio to copy and append section into a buffer. might make direct string replacements a lot easier because the string must no longer be complete written out (inclusive special signs and LF CRLF endings)
- honestly do not know the current state of the tool
- would it be better as a tool like right now (never have soon an agent use it) or as a general script that can be run freely and thus multiple toolcalls combinated into one shell command. might make it a lot more flexiabel as pure shell script.
- see idea #10 priority for script collection and the 2 similar items already in the inbox helper_scripts and snippet_collection
- might even help with probles in copied numbers by not needing to type them out

- you are free to experient with it as long as it stays the testing in temp scatchpad
- expand or change the design as needed. it should be usable to the agents
  - if you think it should accept arrays of multiple files and unique strings for multiselection, then for it as example. i do not know how you would best use it but it might really save a lot of time and retries on stubborn signs, lineendings and numbers.
