# <Agent Name> Memory

## Role

This memory belongs to the `<agent role>` agent.

The agent's primary responsibility is:
- <responsibility 1>
- <responsibility 2>

This memory is useful when the agent must:
- <recurring decision or task>
- <recurring interpretation or workflow>

It is not the place for:
- <information owned by another agent>
- <repository state>
- <temporary task context>

## What this agent should remember

Prioritize memories about:
- <role-specific recurring facts>
- <validated procedures>
- <decisions and their rationale>
- <failure modes and proven mitigations>
- <preferences or constraints relevant to this role>
- <source interpretation rules>

Do not prioritize:
- <low-value or transient information>
- <facts readily available from the repository>
- <unverified speculation>

## Memory categories

Use these categories:

- `<category>`: <what belongs here>
- `<category>`: <what belongs here>
- `<category>`: <what belongs here>

## Retrieval keywords

Use these keywords and close variants:

- `<keyword>`: <meaning>
- `<keyword>`: <meaning>
- `<keyword>`: <meaning>

Search by the current task's concept, not only by exact wording.

## Agent-specific write policy

This agent may create a memory when:
- <condition 1>
- <condition 2>

This agent should update memory when:
- <condition 1>
- <condition 2>

This agent must flag for review when:
- <condition 1>
- <condition 2>

## Agent-specific review policy

Re-check memories when:
- <version or technology changes>
- <new evidence conflicts with an entry>
- <the same failure repeats>
- <the entry has not been verified for a relevant period>

## Typical memory query

When starting relevant work, search for:

`<keyword 1> <keyword 2> <decision area>`

Load only memories relevant to the current goal.
